# Admin authentication (required for /admin access)
# Copy this file to .env.local and update values before running the app.

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password
# Must be at least 32 characters — use a long random string in production
ADMIN_SECRET=replace-with-a-long-random-secret-key-at-least-32-chars
